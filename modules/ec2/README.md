# EC2 Module

Provisions the primary Tableau Server instance with secondary EBS storage.

## Features:
- Provisions EC2 with R5 instance types (optimized for Tableau).
- Attaches 100GB extra EBS volume for data.
- Handles user-data bootstrapping for initialization.